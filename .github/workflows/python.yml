name: Python CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Set up python
      uses: actions/setup-java@v1
      with:
        python-version: 3
    - name: Run tests
      run: pytest -v tests
